export type UserProfile = {
  profilePicture: string;
  coverImage: string;
  bio: string;
  avatarType: string;
  displayName: string;
  followings: number;
  followers: number;
  blockings: number;
  blockers: number;
  registered: boolean;
  confirmed: boolean;
}
